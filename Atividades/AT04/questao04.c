#include <stdio.h>

int[] ler3Numeros(int vet[]);

int main() {
    int vet[3];
    for (int i = 0; i < 3 ; ++i) {
        numero = ler3Numeros(vet[3]);
        printf("numero digitado %d\n\n", numero);
    }
}

int[] ler3Numeros(int vet[]) {
    int numeros[3];
    printf("digite o número\n\n");
    scanf("%d", &numeros[i]);
    return numeros[i];


}